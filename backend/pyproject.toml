[project]
name = "civic-service-request-agent"
version = "0.1.0"
description = "FastAPI backend agent on Vercel"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn"
]

[tool.vercel]
entrypoint = "backend.main:app"
